/*
Write a C program that finds the factorial of a number, provided by the user, using recursion (the function calls itself).


NOTE:
1. Accept positive integers only
2. MUST use a recursive function
3. Display result in the following format ONLY:
   Factorial of <number> = <factorial of number>

Example:
Enter a positive integer: 6
Factorial of 6 = 720
*/

